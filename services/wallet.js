const transactions = require('../data/dbModels/transactions');
const wallets = require('../data/dbModels/wallets');

async function ProcessTransaction(userId, giverId, orgId, shoutoutId, action) {}

module.exports = {
  ProcessTransaction,
};
